
for (var x = 1; x <= 10; x++) {
    console.log("Number = " + x);
}

console.log("-----------------------");

var x = 1
while (x < 10) {
    console.log("while true x  = " + x);
    x++
}

// Do While Loop
do {

} while (condition);

// For in loop
for (const key in object) {
    if (Object.hasOwnProperty.call(object, key)) {
        const element = object[key];
    }
}